// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String
  apiKey    String?  @unique // API Key para dispositivos IoT (ESP32)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relación opcional con datos ambientales
  environmentalData EnvironmentalData[]
}

model EnvironmentalData {
  id        String   @id @default(cuid())
  counter   Int      @unique @default(0) // Contador secuencial para respuesta al ESP32
  tempAmb   Float? // Temperatura Ambiente en °C
  humAmb    Float? // Humedad relativa Ambiental en %
  radiacion Float? // Radiación terrestre (UV)
  humSuelo  Float? // Humedad de Suelo en %
  timestamp DateTime @default(now())

  // Relación con usuario (opcional, si los datos son por usuario)
  userId String?
  user   User?   @relation(fields: [userId], references: [id])
}
